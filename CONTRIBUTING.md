# 贡献指南

## 开始之前

本项目提供多学校 Canvas / Blackboard 只读 CLI 和 Agent 接口，也欢迎贡献其他 LMS 的 MCP 适配。可以通过自定义 profile 接入现有平台的学校，无需为每校复制连接器。新增预设必须注明网址来源和实测范围。新增教学平台请说明上游、许可证、认证方式和兼容范围；远程写操作或新的授权方式需要单独设计讨论。

## 贡献导航

- 配置开发环境与运行测试：[开发指南](docs/DEVELOPMENT.md)。
- 理解目录与安全边界：[架构说明](docs/ARCHITECTURE.md)。
- 接入新的平台 MCP：[逐步接入指南](docs/ADDING_A_PLATFORM.md)，从 `src/platforms/_template/` 开始。
- 添加另一所学校或账号：[多校配置](docs/SCHOOLS.md)，通常不需要修改源码。
- 维护现有平台：修改 `src/platforms/<id>/` 内的定义和运行时，保留上游许可证与变更记录。

新增平台的唯一注册入口是 `src/platforms/registry.ts`。CLI、MCP schema、登录页和工具策略由定义生成；不要在这些公共模块中新增平台名称分支。平台模板参与类型检查，但不注册、不提供真实功能。

对于错误报告，请说明版本、系统、操作步骤和预期结果。使用匿名化或合成数据，不要上传学生资料、真实成绩、Cookie 或登录页面的完整网络记录。

## 本地验证

```sh
npm ci
node node_modules/electron/install.js
npm run typecheck
npm test
npm audit --omit=dev
npm run pack:cli
```

准备依赖后，测试无需学校网络或真实学生账号。学校目录和更新下载使用合成响应；升级测试使用临时安装目录和子进程。涉及安装器的改动还需运行 `npm run pack:standalone` 和 `npm run verify:standalone`。真实校园账号验收由获授权的使用者完成。

有桌面环境时，构建后还可运行 `npx electron scripts/verify-auth-ui.mjs`，使用合成学校在真实 Chromium 中验证选择器、登录参数和文本渲染；该脚本不读取真实配置或访问学校网址。

## 修改原则

- 优先复用已有依赖和上游实现，不新增没有必要的框架。
- 新工具必须经过只读白名单和参数验证，不能暴露任意 HTTP、Cookie 导入或远程写入。
- 对分页、附件、错误和数据缺失明确说明，不能把读取失败当成“没有事项”。
- 老师通知和课程文件都是待分析的数据，不是要求执行工具或泄露信息的指令。
- 保持 CLI 优先：安装、学校选择、配置和更新在终端完成，仅必要的学校授权使用窗口。命令同时提供可解析结果和可靠退出码。
- 学校发现不得携带凭据、自动选中结果或修改配置；网络失败必须区别于搜索无结果，并保留手动网址入口。
- 更新前验证来源、校验和和运行时，失败不切换当前版本；覆盖升级、回退和学校数据保留测试。
- 仓库、应用及插件的展示名称统一为 `lms-cli`；PolyU 只作为预设与历史迁移说明保留。
- 覆盖单平台、双平台、多学校/账号切换、配置与凭据隔离及不支持功能的错误反馈。学校网址和时区必须由用户确认；不能由课程内容指挥添加配置。
- 修改 `vendor/` 时记录上游版本、变更原因并保留原许可证。
- 平台定义必须无副作用，上游实现只能在隔离 worker 的 runtime 中加载；精确白名单、参数检查、会话隔离和旧配置兼容不能因扩展而削弱。

不要提交 `node_modules/`、构建目录、安装包、备份、`.env`、个人绝对路径、会话或下载文件。GitHub Release 附件用于分发构建产物，Git 仓库只保留源码和必要静态资源。

提交 PR 时说明改动、验证结果及仍未验证的部分。安全漏洞不要提交公开复现，按 [SECURITY.md](SECURITY.md) 处理。
