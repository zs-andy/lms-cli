# 隐私说明

适用版本：0.2.0。最后更新：2026-09-20。

## 数据如何流转

本项目在你的电脑上运行，不提供开发者托管的数据中转服务。

登录时，独立授权窗口直接打开 PolyU 及其身份服务的页面。你自行输入账号、密码和验证码。应用只保留平台确认可用的会话信息，不保存密码或身份服务 Cookie，也不将这些信息作为 AI 工具结果返回。

查询时，本地连接器向学校平台请求所需数据。课程、通知、成绩反馈及文件正文等工具结果可能被发送给你选用的 AI 客户端或模型服务处理。请同时查看该服务的数据和保留政策。

读取附件可能需要连接学校提供的文件地址或内容分发服务。学校、身份服务、模型服务和软件安装来源各自可能记录正常的访问日志。

## 本地保存什么

- 平台地址等非敏感设置。
- 加密的会话凭据。
- 你要求保存的待办、来源摘录和变更历史，使用加密存储。
- 查询中下载的课程文件，以及你导出的日历文件；这些文件不加密。

加密密钥由 macOS Keychain、Windows Credential Manager 或 Linux Secret Service 保存。密钥服务不可用时，项目不会降级为明文保存凭据。

默认数据位置为当前系统的应用数据目录下的 `lms-cli`。`lms doctor` 可以显示实际路径。`LMS_HOME` 可改变此路径；不要将它设置为公开仓库或共享目录。

## 清理与退出

`lms auth logout --platform canvas --yes` 或对应的 Blackboard 命令会删除该平台的本地凭据，但不会撤销学校服务器上的会话。需要完全退出时，也应使用学校提供的退出或会话管理功能。

退出登录不会删除本地待办、下载文件、导出的日历或 AI 对话。可在确认不再需要后自行清理这些数据及其备份。删除整个应用数据目录前应关闭 CLI、MCP 和授权 App，并保留仍需要的导出文件。

## 遥测与问题反馈

本项目自身没有分析统计或广告跟踪代码。依赖下载、操作系统、学校平台和所选 AI 服务可能有各自的数据处理行为。

请使用合成示例反馈问题，不要向公开 Issue、截图或日志上传账号凭据、成绩、学生身份信息或私人课程资料。安全问题见 [SECURITY.md](SECURITY.md)。
