# 隐私说明

适用版本：0.4.0。最后更新：2026-09-21。

## 数据如何流转

本项目在你的电脑上运行，不提供开发者托管的数据中转服务。

登录时，授权窗口直接打开你确认的学校网址及其身份服务页面。请核对网址，再自行输入账号、密码和验证码。应用只保留平台确认可用的会话信息，不保存密码或身份服务 Cookie，也不将这些信息作为 Agent 工具结果返回。配置、搜索和更新不需要授权窗口。

查询时，本地连接器向学校平台请求所需数据。通过 Agent 使用时，课程、通知、成绩反馈及文件正文等工具结果会交给你选用的客户端或模型服务处理。直接使用结构化 CLI 查询不会调用模型服务。请同时查看所选服务的数据和保留政策。

读取附件可能需要连接学校提供的文件地址或内容分发服务。学校、身份服务、模型服务和软件安装来源各自可能记录正常的访问日志。

## 学校发现、安装和更新

在线学校搜索向 Canvas 官方公开目录发送你输入的学校名称或域名，不携带学校凭据、不读取个人配置，也不访问搜索结果中的学校私有接口。目录提供方可见查询词及正常网络连接信息。不要输入账号、密码或其他个人资料；使用 `--offline` 可以仅搜索本地预设。Blackboard 当前不发送在线目录请求。

交互式 CLI 使用时和 MCP 会话首次读取配置时，会向固定的 GitHub 公开 Release API 检查版本。请求使用通用 User-Agent，不包含学校、账号、课程或凭据；GitHub 可见正常连接信息。设置 `LMS_UPDATE_CHECK=0` 关闭自动检查，`lms update --check` 仍可主动检查。检查本身不会下载或执行更新。

安装器及经确认的更新从项目 GitHub Release 下载程序和校验和，必要时经过 GitHub 的文件分发服务。开发者源码安装还会从 npm、Node.js 和 Electron 的分发来源获取依赖。

`lms connect codex` 在本机生成插件副本和稳定的启动配置，其中包含程序与状态目录路径，不包含凭据。接入操作通过已安装的 Codex CLI 注册本地插件；替换已有同名插件需要单独确认。安装和更新不会自动设置定时任务或日历订阅。

## 本地保存什么

- 学校/账号配置 ID、显示名称、平台地址与时区，以明文配置保存。不要在显示名称、网址或 ID 中填写密码、令牌或不必要的个人信息。
- 加密的会话凭据。
- 你要求保存的待办、来源摘录和变更历史，使用加密存储。
- 查询中下载的课程文件，以及你导出的日历文件；这些文件不加密。

加密密钥由 macOS Keychain、Windows Credential Manager 或 Linux Secret Service 保存。密钥服务不可用时，项目不会降级为明文保存凭据。

同一安装中的多个学校/账号共享系统保管的加密主密钥，但加密文件按 profile、平台与地址绑定，不能互换。查询缓存、本地待办与下载目录也按 profile 区分。这不是不同操作系统用户之间的安全隔离；不要共享你的系统账号。

默认数据位置为当前系统的应用数据目录下的 `lms-cli`。`lms doctor` 可以显示实际路径。`LMS_HOME` 可改变此路径；不要将它设置为公开仓库或共享目录。

## 清理与退出

`lms --profile <id> auth logout --platform canvas --yes` 或对应的 Blackboard 命令会删除该配置、该平台的本地凭据，但不会撤销学校服务器上的会话。省略 `--profile` 则使用当前默认配置。需要完全退出时，也应使用学校提供的退出或会话管理功能。

退出登录不会删除本地待办、下载文件、导出的日历或 Agent 客户端中的历史记录。可在确认不再需要后自行清理这些数据及其备份。程序升级和回退不删除学校数据；删除整个数据目录前应关闭 CLI、MCP 和授权窗口，并保留仍需要的导出文件。

## 遥测与问题反馈

本项目自身没有分析统计或广告跟踪代码。软件与依赖分发服务、学校目录、操作系统、学校平台和所选 Agent 服务可能有各自的数据处理行为。

请使用合成示例反馈问题，不要向公开 Issue、截图或日志上传账号凭据、成绩、学生身份信息或私人课程资料。安全问题见 [SECURITY.md](SECURITY.md)。
